# Allow the built-in 'guest' user to connect from other containers.
# By default RabbitMQ restricts 'guest' to localhost only, which makes
# cross-container connections fail with: 403 ACCESS_REFUSED.
# The broker has no host ports published (internal-only on arhan-network),
# so relaxing this is safe here.
loopback_users.guest = false
